import styled from 'styled-components';

export const Button = styled.button`
    padding: 0.33em 1em;
    margin-bottom: 1em;
    font-size: 1em;
    border: 2px solid transparent;
    border-radius: 12px;

    &:hover {
        cursor: pointer;
    }
`;
